# Claude Code / AI tooling
.claude
ai-context

# Editors / IDEs
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.sw?

# OS
.DS_Store
Thumbs.db

# Secrets / local config — every service's, plus infra's and each frontend's
.env
.env.local
.env.*.local
!.env.example

# k8s secret input (see k8s/shared/.env.k8s.example) — same convention as root .env above
k8s/shared/.env.k8s

# Java / Maven (services/*/target)
target/
hs_err_pid*.log

# Node / Vite (frontend/*, e2e)
node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright (e2e)
test-results/
playwright-report/

# run-all.sh / stop-all.sh runtime output
/logs/
/pids/

# Fetched once locally for Docker builds (see README) — not committed, it's a
# third-party binary, not source.
/services/otel-agent.jar

/video-edit-clips

/Notes
