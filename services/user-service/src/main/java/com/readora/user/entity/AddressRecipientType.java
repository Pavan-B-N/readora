package com.readora.user.entity;

/** OWNER — the account holder receives it themselves (name/phone pre-filled from their profile). GUEST — someone else does. */
public enum AddressRecipientType {
    OWNER,
    GUEST
}
