package com.readora.user.entity;

// The reason a wallet transaction was created.
public enum WalletTransactionType {
    SIGNUP_BONUS,
    REFERRAL_BONUS,
    REDEEMED,
    REVERSED,
    TOPUP,
    COUPON_REDEEMED,
    CASHBACK
}
