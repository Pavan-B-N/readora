apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-admin
  namespace: readora
  labels:
    app: frontend-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-admin
  template:
    metadata:
      labels:
        app: frontend-admin
    spec:
      containers:
        - name: frontend-admin
          image: readoracontainerregistry.azurecr.io/readora-frontend-admin:latest
          ports:
            - containerPort: 80
          # nginx serves a SPA — / always returns index.html once the container is up.
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
