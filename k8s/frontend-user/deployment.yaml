apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-user
  namespace: readora
  labels:
    app: frontend-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-user
  template:
    metadata:
      labels:
        app: frontend-user
    spec:
      containers:
        - name: frontend-user
          image: myreadoraregistry.azurecr.io/readora-frontend-user:latest
          ports:
            - containerPort: 80
          # nginx serves a SPA — / always returns index.html once the container is up.
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
