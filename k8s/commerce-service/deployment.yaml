apiVersion: apps/v1
kind: Deployment
metadata:
  name: commerce-service
  namespace: readora
  labels:
    app: commerce-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commerce-service
  template:
    metadata:
      labels:
        app: commerce-service
    spec:
      containers:
        - name: commerce-service
          image: myreadoraregistry.azurecr.io/readora-commerce-service:latest
          ports:
            - containerPort: 8084
            - containerPort: 9000
              name: management
          envFrom:
            - configMapRef:
                name: readora-config
            - secretRef:
                name: readora-secrets
          # httpGet against the separate management port — see application.yml for why it's isolated.
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9000
            initialDelaySeconds: 45
            periodSeconds: 15
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 750m
              memory: 768Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
